using UnityEngine;

namespace PrettyLogSystem
{
    public static class PrettyColors
    {
        // Neon / Soft Palette tailored specifically for console text readability
        public static readonly Color Red      = new Color(1f, 0.3f, 0.3f);     // Soft Stark Red
        public static readonly Color Green    = new Color(0.4f, 1f, 0.4f);     // Vivid Mint Green
        public static readonly Color Blue     = new Color(0.3f, 0.6f, 1f);     // Sky Blue
        public static readonly Color Cyan     = new Color(0.2f, 0.9f, 1f);     // Vibrant Cyan
        public static readonly Color Magenta  = new Color(1f, 0.4f, 1f);       // Soft Magenta
        public static readonly Color Yellow   = new Color(1f, 0.9f, 0.2f);     // Bright Yellow
        public static readonly Color Orange   = new Color(1f, 0.6f, 0.1f);     // Pastel Orange
        public static readonly Color Purple   = new Color(0.6f, 0.4f, 1f);     // Deep Pastel Violet
        public static readonly Color Pink     = new Color(1f, 0.6f, 0.8f);     // Hot Pink
        public static readonly Color Gray     = new Color(0.7f, 0.7f, 0.7f);   // Clean Slate Gray
    }
}